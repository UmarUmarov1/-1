// задание 1
let message = "ты пацан";
const userAge = 20;
let age = 35;
if (userAge < age); {
    console.log(message);
}
//задание 2
let message = "доступ запрещен";
const isAdmin = false;
let admin = true;
if(isAdmin < admin) {
console.log(message);   
}

//задание 3
let message = "правильный пароль"; 
const password = "sherlock";
const realPassword = "holmes";
if(password == realPassword) {
    console.log(message);
}

// задание 4
let message = "возраст указан верно"
const myAge = "20";
if(typeof myAge === typeof 0) {
console.log(message);
    }
    // задание 5
    let message = "икс меньше игрик";
const x = 2.7;
const y = -6;
if(x < y) {
    console.log(message);
}
